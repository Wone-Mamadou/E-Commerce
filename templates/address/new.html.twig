{% extends 'base.html.twig' %}

{% block title %}New Address{% endblock %}

{% block body %}
{{ include("partiel/title_section.html.twig", {'name':'Nouvelle adresse '}) }}
<div class="login_register_wrap section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-6 col-md-10">
                <div class="login_wrap">
                    <div class="padding_eight_all bg-white">
                        <div class="heading_s1">
                            <h3>Ajout d'adresse</h3>
                        </div>
                        <div class="content">
                            {{ include('address/_form.html.twig') }}
                            <a class="btn btn-border-fill btn-block mt-2" href="{{ path('app_mon_compte') }}">Rerour au Compte</a>
                        </div>                           
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

    
{% endblock %}
